//Design a program that writes the messages "Program Start" and "End" in the screen, one on each line, using only a printf() command.

#include<stdio.h>
#include<stdlib.h>

int main(){
    printf("Inicio do programa\nFim");

    system("pause");
    return 0;
}
