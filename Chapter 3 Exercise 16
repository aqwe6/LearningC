//Write a program that reads an integer and shows multiplication and divide this number by two (use the bit offset operators).

#include<stdio.h>
#include<stdlib.h>

int main(){
    int a;
    scanf("%d",&a);
    printf("%d\n%d\n",a*=4,a/=2);

    system("pause");
}
