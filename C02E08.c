//Make a program that reads two integers and then prints them in the reverse order in which they were read.

#include<stdio.h>
#include<stdlib.h>

int main(){
    int a,b;
    scanf("%d %d",&a,&b);
    printf("%d %d",b,a);

    system("pause");
    return 0;
}
