// Read the height and radius of a circular cylinder and print the volume of that cylinder. The volume of a circular cylinder is calculated using the following formula:
//V = π * radius2 * height
//where π = 3.141592

#include<stdio.h>
#include<stdlib.h>

int main(){
    float x,y;
    printf("height?");
    scanf("%f",&x);
    printf("radius?");
    scanf("%f",&y);
    printf("volume of a circular culinder is %f",3.141592*x*y*y);

    system("pause");
}
